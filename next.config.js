/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: '/pet-nftify',
    reactStrictMode: true,
    images: {
      unoptimized: true,
    },
  }
  
  module.exports = nextConfig