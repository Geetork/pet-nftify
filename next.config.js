/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: '/pet-nftify',
    images: {
      domains: ['geetork.github.io'],   
    },
  }
  
  module.exports = nextConfig